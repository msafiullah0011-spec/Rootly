import { ProfileScreen } from '@/features/settings/components/profile-screen';

/** Frame 9's account card — the profile behind it. */
export default function ProfileRoute() {
  return <ProfileScreen />;
}
