import { ConnectedAccountsScreen } from '@/features/settings/components/connected-accounts-screen';

/** Settings → Connected accounts. */
export default function ConnectedAccountsRoute() {
  return <ConnectedAccountsScreen />;
}
