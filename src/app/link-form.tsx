import { LinkFormScreen } from '@/features/links/components/link-form-screen';

/** Frame 12 — manual add / edit. Presented modally by the root stack. */
export default function LinkFormRoute() {
  return <LinkFormScreen />;
}
