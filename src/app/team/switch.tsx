import { WorkspaceSwitcherSheet } from '@/features/team/components/workspace-switcher-sheet';

/** Frame 15's workspace name. Presented as a transparent modal by the root stack. */
export default function WorkspaceSwitchRoute() {
  return <WorkspaceSwitcherSheet />;
}
