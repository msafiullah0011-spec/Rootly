import { TimelineFilterSheet } from '@/features/timeline/components/timeline-filter-sheet';

/** Frame 14's filter button. Presented as a transparent modal by the root stack. */
export default function TimelineFilterRoute() {
  return <TimelineFilterSheet />;
}
