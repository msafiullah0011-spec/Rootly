import { QuickAddSheet } from '@/features/links/components/quick-add-sheet';

/** Frame 6 — Add flow. Presented as a transparent modal by the root stack. */
export default function QuickAddRoute() {
  return <QuickAddSheet />;
}
