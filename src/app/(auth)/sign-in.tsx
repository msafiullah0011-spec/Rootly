import { SignInScreen } from '@/features/auth/components/sign-in-screen';

/** Frame 13 — Auth. Route file stays thin; the screen lives in the feature. */
export default function SignInRoute() {
  return <SignInScreen />;
}
